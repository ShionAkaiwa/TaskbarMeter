# TaskbarMeter 説明書

はじめて使う人は [はじめかた.md](はじめかた.md) を先に読んでください。
こちらは全部の機能をひととおり説明したものです。

- [1. アイコンの読みかた](#1-アイコンの読みかた)
- [2. 表示する項目を選ぶ](#2-表示する項目を選ぶ)
- [3. 見た目を変える](#3-見た目を変える)
- [4. 使用率を記録してグラフにする](#4-使用率を記録してグラフにする)
- [5. 見守り通知](#5-見守り通知)
- [6. 隠す・出す](#6-隠すだす)
- [7. 困ったとき](#7-困ったとき)
- [8. やめかた](#8-やめかた)

---

## 1. アイコンの読みかた

アイコン 1 つが指標 1 つです。CPU と GPU を選べば、アイコンは 2 つ並びます。

### 下の帯（ゲージ）

すべての見た目に共通で、**下の帯が使用率**です。右に伸びるほど忙しい。

| 帯の状態 | 意味 |
|---|---|
| ほとんど伸びていない | ひま |
| 半分くらい | ふつうに働いている |
| **赤くなった** | 85% 以上。かなり限界に近い |

赤くなるのは帯だけで、アイコン本体の色は変わりません。
これは意図的で、本体まで赤くすると CPU も GPU も同じ色に見えてしまい、
**一番知りたい「どれが限界なのか」が分からなくなる**ためです。

### キャラの表情

「ドット絵キャラ」を選んでいる場合、負荷で 5 段階に変わります。

| 使用率 | 表情 | 見た目 |
|---|---|---|
| 20% 未満 | うとうと | 目を閉じて zzz |
| 20〜50% | ふつう | まる目 |
| 50〜80% | ごきげん | ^ ^ の笑い目 |
| 80〜95% | あせり | 見開き＋汗 |
| 95% 以上 | 限界 | × 目＋大きく開いた口＋汗 2 つ |

ときどきまばたきします。

### 「待ちぼうけ」の顔

**自分は暇なのに、隣の誰かが限界まで働いているとき**だけ出る顔があります。
眠らずに、目をよそへ向けて「…」を出します。

これは見た目のお遊びではなく、**どこで詰まっているかを教えてくれる**ものです。

| こうなっていたら | 詰まっているのは |
|---|---|
| **GPU(学習) が待ちぼうけ + CPU が限界** | データの前処理。`num_workers` を増やす、前処理を軽くする |
| **GPU(学習) が待ちぼうけ + ディスクが限界** | データの読み込み。SSD に移す、キャッシュする |
| GPU も CPU もごきげん | うまく回っています |

学習が思ったより遅いとき、まずここを見てください。
GPU が寝ているのに CPU だけ真っ赤なら、GPU を増やしても速くなりません。

### マウスを乗せる

正確な数値が出ます。`CPU 42%`、`VRAM 5.2 / 8.0 GB (65%)` のように、
アイコンには入りきらない情報もここで見られます。

---

## 2. 表示する項目を選ぶ

アイコンを右クリック →「設定…」→ **① 表示する項目**。

| 項目 | 何を見ているか | こんなときに |
|---|---|---|
| CPU 使用率 | CPU 全体 | いつでも。基本 |
| メモリ使用率 | 物理メモリ | データを読み込みすぎていないか |
| GPU 使用率 (3D) | 描画・ゲームの負荷 | ゲーム、動画編集 |
| **GPU 使用率 (学習)** | **Compute / CUDA の負荷** | **深層学習の学習中** |
| VRAM 使用量 | GPU メモリの使用量 | OOM で落ちる前に気づきたい |
| ディスク使用率 | ディスクの忙しさ | データ読み込みが詰まっていないか |
| ネット受信 / 送信 | 通信速度 (Mbps) | ダウンロード中 |

### 深層学習をする人へ

**学習の負荷は「GPU 使用率 (3D)」には出ません。**
3D エンジンは描画用で、学習は Compute エンジンを使います。
3D だけ見ていると、学習が回っていても 0% のままに見えます。

学習中に見るべきは **「GPU 使用率 (学習)」** と **「VRAM 使用量」** の 2 つです。
VRAM が上限に近づいていたら、バッチサイズを下げる判断ができます。

### 色を変える

右クリック →「色」→ 指標を選ぶと色を選べます。
同じ色が並ぶと見分けがつかないので、離れた色にするのがおすすめです。
「すべて既定の色に戻す」で元に戻せます。

---

## 3. 見た目を変える

設定画面の **② 見た目**、または右クリック →「見た目」。

### 数字

使用率をそのまま数字で出します。正確な値を横目で追いたいときに。
100% は桁が入らないので `99` と表示します（帯は満タンになります）。

### ドット絵キャラ（既定）

表情で状態が分かります。ちらっと見るだけで様子が掴めるので、ふだん使いに向いています。

### 好きな画像

手持ちの画像をドット絵アイコンに変換して使えます。

1. 設定画面の「好きな画像」を選ぶ（または「画像を選び直す…」）
2. 画像ファイルを選ぶ
3. プレビュー画面で調整して「これにする」

| 設定 | 内容 |
|---|---|
| 解像度 | 16〜64。トレイの実表示は 16〜32px なので、大きくしても効くのは高 DPI 環境のみ |
| 背景を自動で透明にする | 四隅に近い色を背景とみなして抜く。単色背景の画像でよく効く |
| 色を減らしてドット絵っぽくする | 色数を落とす。写真をアイコンらしくしたいときに |
| なめらかに表示する | オフにするとドットがくっきり出る |

プレビューには実寸（16 / 24 / 32px）も並ぶので、**小さくしたときに何か分かるか**を必ず確認してください。
顔のアップなど、大きな形が単純な画像がうまくいきます。

---

## 4. 使用率を記録してグラフにする

右クリック →「**計測を開始**」。記録中はアイコンの左上に赤い点がつきます。

もう一度右クリック →「計測を停止して結果を見る」で結果画面が出ます。

- **折れ線グラフ** — 表示中の全指標を重ねて表示
- **表** — 指標ごとの平均・最大・最小
- **CSV で保存** — Excel で開ける形式（BOM 付き UTF-8 なので文字化けしません）

CSV は `時刻, 経過秒, 各指標の実値` の形です。
実値なので、VRAM なら GB、ネットなら Mbps がそのまま入ります。

長時間まわしても大丈夫です。サンプルが増えすぎると自動で間引いて解像度を半分に落とすので、
一晩まわしてもメモリを食い潰しません（そのぶん記録間隔は粗くなります）。

**使いどころ**：学習を回す前に計測を開始しておくと、
「GPU が本当に張り付いていたか」「VRAM がどこまで行ったか」を後から確認できます。

---

## 5. 見守り通知

### 処理が終わったかも

計測中に、GPU が一度高負荷になったあと **3 分間ほぼ 0%** が続くと、
「処理が終わったかもしれません」と通知します。

席を外している間にジョブが終わった、あるいは落ちたことに気づくための機能です。
計測を開始していないと動きません。

### VRAM がいっぱい

**VRAM 使用量を表示していると**、VRAM が **90% を 5 秒以上** 超えたときに警告します。

学習中の OOM は落ちてから気づくことがほとんどですが、数十秒でも早く分かれば、
バッチサイズを下げて回し直す判断ができます。エポックの終わりに落ちて
何時間か無駄にする、というのを避けるためのものです。

一度下がって 80% を切ると、また鳴るようになります
（90% 付近を行き来するたびに鳴り続けないようにしてあります）。

VRAM の総容量が読めない環境では、誤検知を避けるため何もしません。

---

## 6. 隠す・出す

| やりたいこと | 方法 |
|---|---|
| いますぐ隠す | **Ctrl + Alt + M**（もう一度押すと戻る） |
| ふだんは隠して、忙しいときだけ出す | 右クリック →「高負荷のときだけ表示」 |
| 何 % から出すか変える | 右クリック →「表示に切り替わる負荷」 |
| 見失った | **exe をもう一度ダブルクリック**（設定画面が開きます） |

発表中やスクリーンショットを撮るときは Ctrl+Alt+M が便利です。

---

## 7. 困ったとき

### アイコンがタスクバーに出てこない

Windows 11 は新しいアイコンを **「∧」ボタンの中**に隠します。アプリは動いています。

1. 設定画面の **③ タスクバーに出す** のボタンを押す
2. 効かないときは、**∧** を押して、出てきたアイコンを**タスクバーへドラッグ**する

ドラッグは確実です。一度出せば次からは出たままになります。

### 「終了」したら戻せなくなった

終了するとアプリは動いていないので、Ctrl+Alt+M も効きません。

- **スタートメニューで「TaskbarMeter」と検索**してください（初回起動時に登録されています）
- 見つからなければ、exe を直接ダブルクリックしてください

### GPU が 0% のまま動かない

- 学習中なら「GPU 使用率 **(学習)**」を表示してください（→ [2 章](#2-表示する項目を選ぶ)）
- それでも 0% なら、タスク マネージャーの「パフォーマンス」→ GPU で
  どのエンジンが動いているか確認してください

### 「取得不可」と出る

パフォーマンスカウンタが壊れている可能性があります。
管理者権限のコマンドプロンプトで次を実行すると直ることがあります。

```
lodctr /R
```

### 数字が全部おかしい / 動かない

設定を初期状態に戻せます。アプリを終了してから、
レジストリ エディタで次のキーを削除してください。

```
HKEY_CURRENT_USER\Software\TaskbarMeter
```

次に起動すると初回セットアップからやり直しになります。

---

## 8. やめかた

1. 右クリック →「終了」
2. 設定画面で「Windows を起動したら自動で始める」のチェックを外す
3. 設定画面で「スタートメニューに追加する」のチェックを外す
4. exe ファイルを削除する

設定を完全に消したい場合は、上の 7 章にあるレジストリのキーと、
`%LOCALAPPDATA%\TaskbarMeter`（画像から作ったアイコンの保存先）も削除してください。

このアプリは管理者権限を使わず、ユーザー領域にしか書き込みません。
システムを変更していないので、消せばきれいに元通りになります。
