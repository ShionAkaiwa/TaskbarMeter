# 改善記録

「読んで洗い出す → 裏を取って直す → 余地が減ったらリリース」を 1 周とした記録。
**残っている課題**は下にまとめてある。直したいものを選んで指示すること。

進めかた

1. **実測** — 分担して読み、不具合と改善案を挙げる。推測ではなく該当箇所を根拠にする
2. **改善** — 自分でコードを見て裏を取ってから直す。見た目は必ず描いて目で見る
3. **リリース** — 余地が減ったら `release.cmd` → GitHub Releases

---

## 第 1 回（2026-08-20）

読んだ範囲は 4 つ。キャラの描画とアニメーション / 設定画面 / アプリの挙動とリソース /
配布とドキュメント。以下はすべて、直す前にコードで裏を取ったもの。

### キャラを作り直した

段階ごとに**体の形そのもの**を変えた。16px では目や口は 2〜3 ドットしかなく、
実際に描き出して並べると表情の違いは実寸でほとんど読めない。先に目に入るのは輪郭のほう。
うとうとの 8 行から限界の 14 行まで、負荷が上がるほど背が伸びる。頭の毛も一緒に立つ。

| 直したもの | なぜ |
|---|---|
| 段階にヒステリシス（3% の遊び）を入れた | 段階ごとに形が変わるので、境目に張り付いた負荷で 1 秒ごとに体が跳ねる。撤去済みの「震え」を作り直していた |
| ゲージの赤を 85%→92% の連続変化に | 切り替え式だと 85% を挟んで揺れる負荷で色が点滅する |
| zzz を 4x4 の Z に | 3x3 では z と 工 の区別が付かない。ゆっくり浮き上がる |
| 汗を輪郭に重ねないように | 丸いシルエットが欠けて見えていた |
| 限界の口を楕円に | 真四角の黒は、口ではなく体に空いた穴に見える |
| まばたきを 13 秒間隔・指標ごとにずらした | 1 tick は 1 秒。9 秒間隔だと 1 割の時間ずっと目を閉じている。8 個が一斉に閉じると機械に見える |
| 設定画面のプレビューが必ず閉じ目だった | `IconMood` の既定は Tick=0 で、0 は 9 の倍数 |
| 計測中の赤ドットを点滅させた（2 秒点いて 1 秒消える） | 出しっぱなしだと録れているか分からない |

### メーターを読みやすくした

| 直したもの | なぜ |
|---|---|
| 下の行だけ半マス先に伸ばして 28 段階に | 14 マスでは 1 マス 7%。42% と 48% が同じ絵だった |
| 半分の位置に切り欠きを入れた | 長さだけでは割合が読めない。塗ったところには入れない（満タンのバーが欠けて見える） |
| 未達部分の色を濃くした | 明るいタスクバーで薄すぎて、どこまで伸びているか読めなかった |
| 1〜3% でも 1 マス点けるように | 切り捨てで 0 マスになり、止まっているのと見分けが付かなかった |

### 指標が壊れていたのを直した

| 直したもの | なぜ |
|---|---|
| **GPU が 5 秒ごとに 0% に落ちていた** | 張り替えでカウンタを全部作り直していた。これらは前回読んだ時点との差で値を出すので、作り直すと基準が消える。生き残っているぶんは残すようにした |
| 一度読めなくなった指標が二度と戻らなかった | スリープ復帰・ドライバ更新で例外が出ると、再起動するまで「取得不可」のままだった。1 分後に掛け直す |
| 「読めなくて 0」と「本当に 0%」を区別するようにした | 見守り通知が、動いている最中に「終わったかも」と言い出す原因 |
| ネット速度から仮想 NIC を除外 | WSL2 が入っていると同じ通信が 2 回数えられて速度が倍になる |

### アプリの挙動

| 直したもの | なぜ |
|---|---|
| `Update()` を「読む」と「描く」に分けた | メニュー操作のたびに読み直していたので、設定をいじっている間ずっと CPU が 0% に見えていた |
| 起動のたびにタスクバー表示を掛け直すように | 設定にはそう書いてあったが、実際には設定画面を開いたときしか掛かっていなかった |
| 初回はタスクバー表示を既定で有効に | ③ のボタンを押し忘れると一度も掛からず、∧ の中に入ったままになる。配布でいちばんつまずくところ |
| exe を動かしたらショートカットと自動起動を貼り直す | 移すと消えた場所を指したまま残り、チェックは付いているのに効かない |
| アイコンを全部消せてしまう穴を塞いだ | 設定画面が自分のチェックを数えていた。開いたままメニューからも外せるので、数が古くなる |
| exe 再起動で立てた強制表示を、設定画面を閉じたら下ろす | 下ろさないと「高負荷のときだけ表示」が効かなくなる |
| 隠れている間の通知を捨てないように | VRAM の警告が、いちばん気づきたい状況（席を外している）で消えていた |
| 長い記録の間引きを O(n) に | 20000 点で 5 時間半に 1 回、アイコンが 1 秒近く止まっていた |
| 記録中は表示を切った指標も読み続ける | 0 が並んで、グラフと平均が実際より低く出ていた |
| 指標 1 つの例外で全体が止まらないように | 毎秒の処理なので、止まると右クリックする先も無くなる |
| 色を毎秒レジストリから読むのをやめた | 8 指標なら 1 時間で 28800 回 |
| 画像モードの Bitmap をキャッシュ | 64x64 × 8 指標で毎秒 3 万回以上 SetPixel を回していた |

### 設定画面

| 直したもの | なぜ |
|---|---|
| 計測結果の画面を拡大表示に対応させた | ここだけ px 決め打ちだった。150% で集計表の下の行が隠れ、指標名も切れる |
| 「高負荷のときだけ表示」中はその旨を出す | 項目を選んでも何も出てこないので、壊れたと思われる |
| ④ に「右クリックで戻れる」「Ctrl+Alt+M」を書いた | 復帰手段 5 系統のうち 2 つしか載っていなかった |
| 画像を選び直したらラジオも合わせる | 見た目が「画像」に変わるのに、ラジオはキャラのままだった |
| 画像フィルタから webp を外した | GDI+ が読めない。勧めておいて「パラメーターが正しくありません」で落ちる |
| 終了の確認の既定を「キャンセル」に | Enter だけで常駐が落ちていた |

### 配布とドキュメント

| 直したもの | なぜ |
|---|---|
| `release.cmd` の各手順を検査するようにした | `-Force` が無く、失敗しても古い ZIP が残って「成功」に見えていた |
| `build.cmd` / `release.cmd` が exe の終了を待つように | 固定 2 秒待ちで、失敗すると MSBuild のエラーの山になっていた |
| 同梱テキストを BOM 付き UTF-8 / CRLF で書き出す | 展開後いちばん最初に開かれる日本語ファイル |
| `LICENSE`（MIT）と免責を追加 | 責任の所在がどこにも書かれていなかった |
| 待ち時間の案内を実測に合わせた | 「3 秒」と書いてあったが実測 6 秒。4 秒目で連打されていた |
| アンインストール手順を順序に依存しない形に | exe を先に消すと、以降の手順が実行できなかった |
| 消し残るものを明記した | `NotifyIconSettings` は**手で消さない**こと（消すと入れ直してもタスクバーに出せなくなる） |
| ダウンロードを止められたとき／隔離されたときを追記 | ここで止まると、この先が全部始まらない |
| 同梱テキストの「くわしい説明」の行き先を変えた | ダウンロード済みの人をダウンロードページに送り返していた |

---

## 残っている課題

**直したいものを番号で指示すること。** 上から順に効くものを並べてある。

### アプリの挙動

| # | 重要度 | 内容 |
|---|---|---|
| 1 | 中 | ネット速度と VRAM（総量が読めない環境）のゲージ上限が下がらない。一度大きい値を観測すると以後ほぼ動かず、グラフの縦軸も記録中に変わる |
| 2 | 中 | 設定画面を開いたまま右クリックメニューを操作すると、設定画面の表示が古いまま残る |
| 3 | 中 | 設定画面が開いていると、exe を再起動しても何も起きない（前面に出てこない） |
| 4 | 中 | GPU のインスタンス列挙が 5 秒ごと。15〜30 秒に落として構わない |
| 5 | 低 | ディスクの `% Disk Time` は 100% を超えることがあり、重い I/O 中はほぼ常時 100% で飽和する |
| 6 | 低 | ネット速度が 100Mbps を超えると常に `99+` |
| 7 | 低 | 経過時間が `DateTime.Now` 基準。時刻変更や夏時間で CSV の経過秒がずれる |
| 8 | 低 | 二重起動防止の名前が既定のまま。昇格の有無が違うと 2 つ目が例外で落ちる |

### 設定画面・見た目

| # | 重要度 | 内容 |
|---|---|---|
| 9 | 高 | 画像→ドット絵の画面がスクロールしない。小さいノートで「これにする」が画面外に出る |
| 10 | 中 | 画像プレビューの描画が固定 px。拡大率の違うモニタへ移すと絵が左上に寄る |
| 11 | 中 | 色の変更と計測が設定画面から辿れない（右クリックメニューにしかない） |
| 12 | 中 | 「タスクバーに出す」を押すと結果の文字が横に伸びて、窓からはみ出す |
| 13 | 低 | 画像の読み込みに失敗しても「これにする」が押せる（押しても何も起きない） |
| 14 | 低 | 選べる色に明るさの制限がない。黒に近い色だと暗いタスクバーで消える |
| 15 | 低 | フォントの解放漏れ（アプリの寿命ぶんなので実害は小さい） |

### アイコン

| # | 重要度 | 内容 |
|---|---|---|
| 16 | 中 | 画像モードのマス計算がキャラモードと 0.5px ずれる。125% / 175% でマークの位置が違う |
| 17 | 低 | 計測中の赤ドットと汗が左上で縦に並び、1 本の帯に見える |
| 18 | 低 | 画像の半透明の縁が不透明に潰れ、1px の縁が残る |

### 配布

| # | 重要度 | 内容 |
|---|---|---|
| 19 | 中 | バージョンを名乗る手段がない。「どの版を使っているか」を確認できない |
| 20 | 低 | 拡大率の違う 2 枚のモニタで、タスクバー側の DPI に追随しないことがある |

---

## ほかのページ

| | |
|---|---|
| **[説明書.md](説明書.md)** | 全機能の説明と、困ったときの対処 |
| **[仕様書.md](仕様書.md)** | 中身がどう動いているか（技術的な話） |
| **[はじめかた.md](はじめかた.md)** | 導入して使いはじめるまで |
| [README](README.md) | このアプリの概要とダウンロード |
