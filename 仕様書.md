# TaskbarMeter 仕様書

Windows の通知領域（タスクバー右下）に、CPU / GPU / VRAM などの使用率を
アイコンとして常時表示する常駐アプリケーションの技術仕様。

対象読者は、中身の仕組みを知りたい人。
使いかたは [説明書.md](説明書.md)、導入手順は [はじめかた.md](はじめかた.md) を参照。

---

## 1. 設計方針

深層学習の学習中に、ウィンドウを切り替えずに GPU の様子を把握したい、というのが出発点。
さらに、同じ研究室の人にそのまま渡せることを条件にした。この 2 つから次を制約とした。

| 方針 | 理由 |
|---|---|
| exe 1 個で動く（インストール不要） | 配布相手に手順を踏ませない |
| 管理者権限を要求しない | 共用 PC や研究室の PC でも使える |
| ネットワーク通信を一切しない | 何をしているか説明でき、持ち込みの障壁を下げる |
| 外部ライブラリを増やさない | ビルドが壊れにくく、中身を追える規模に保つ |
| 温度は測らない | 温度取得には管理者権限が要る（後述） |

### 開発環境

- .NET 8 / C# / WinForms、単一プロジェクト・単一ファイル（`Program.cs`、約 2500 行）
- 外部依存は `System.Diagnostics.PerformanceCounter` のみ
- 検証環境：Windows 11（日本語ロケール）、NVIDIA RTX 3060 Ti（VRAM 8GB）

### 配布形態

`TaskbarMeter.csproj` で self-contained / single-file を指定し、
.NET ランタイムを同梱した exe 1 個を生成する。

```xml
<RuntimeIdentifier>win-x64</RuntimeIdentifier>
<SelfContained>true</SelfContained>
<PublishSingleFile>true</PublishSingleFile>
<IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
<EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
```

出力は約 70MB。受け取る側は .NET のインストールが不要になる。

---

## 2. 全体構成

ウィンドウを持たない常駐アプリなので、`Form` ではなく `ApplicationContext` を起点にする。

```
Program.Main
  └ MeterContext : ApplicationContext        … 本体。1 秒ごとのタイマーで駆動
       ├ List<Metric>                        … 指標。1 指標 = 1 トレイアイコン
       ├ Dictionary<string, Slot>            … 指標ごとの NotifyIcon と現在のアイコン
       ├ IconFactory                         … アイコン画像の生成
       ├ SetupForm                           … 初回セットアップ兼 設定画面
       ├ TrayPromotion / StartMenuShortcut   … 見失わないための仕掛け
       ├ SessionRecorder / SessionResultForm … 計測とグラフ表示
       ├ PixelSkin / SkinPreviewForm         … 画像 → ドット絵アイコン変換
       ├ HotkeyWindow                        … Ctrl+Alt+M
       ├ PerfNames                           … カウンタ名のローカライズ解決
       └ Settings / AutoStart                … HKCU への保存
```

### 二重起動の防止と復帰

名前付き Mutex で単一インスタンスを保証する。
2 つ目が起動されたときは終了するだけでなく、**名前付き Event を叩いて
常駐中のインスタンスに知らせる**。これが「見失ったときに exe をもう一度叩けば戻る」動作になる。

```csharp
using var mutex = new Mutex(true, "TaskbarMeter_SingleInstance", out bool isNew);
var showRequest = new EventWaitHandle(false, EventResetMode.AutoReset, "TaskbarMeter_ShowRequest");
if (!isNew)
{
    showRequest.Set();   // すでに動いている本体に「出てきて」と伝える
    return;
}
```

受け取る側は毎秒のタイマーで非ブロッキングに確認する。

```csharp
if (_showRequest.WaitOne(0))
{
    _forceShow = true;
    _forceHide = false;
    _pendingSettings = true;   // わざわざ起動し直した = 見失っている
}
```

---

## 3. 指標の取得

`Metric` 抽象クラスを継承し、`MetricCatalog.CreateAll()` に登録すると、
メニュー・色設定・計測記録に自動で載る。指標の追加はここだけで完結する。

```csharp
internal abstract class Metric : IDisposable
{
    public abstract string Id { get; }          // 設定キーに使う識別子
    public abstract string Name { get; }
    public abstract Color DefaultColor { get; }
    public virtual bool DefaultEnabled => false;
    public virtual string ShortName => Name;    // 幅の狭い UI 用

    public virtual void Refresh() { }           // カウンタの張り替えが要る指標だけ実装
    public abstract Sample Read();
}
```

`Read()` が返す `Sample` は 5 つの値を持つ。

```csharp
internal readonly record struct Sample(
    double Ratio,    // 0.0〜1.0。ゲージと表情の判定に使う
    string Text,     // アイコンに描く文字
    string Tooltip,  // マウスを乗せたときの表示
    double Value,    // 記録・CSV 用の実値
    string Unit);    // "%" / "GB" / "Mbps"
```

`Ratio` と `Value` を分けているのは、VRAM やネットのように
「割合」と「実際の量」の両方に意味がある指標があるため。
ゲージは `Ratio`、CSV は `Value` を使う。

### 3.1 CPU — `GetSystemTimes`

パフォーマンスカウンタを使わず、Win32 API の差分から求める。
カウンタ破損やロケールの影響を受けないため、最も壊れにくい。

```csharp
[DllImport("kernel32.dll", SetLastError = true)]
private static extern bool GetSystemTimes(out long idle, out long kernel, out long user);
```

前回値との差分を取り、`(全体 - アイドル) / 全体` で使用率を出す。
`kernel` にはアイドル時間が含まれる点に注意が必要で、次のように扱っている。

```csharp
long dIdle = idle - _idle;
long total = (kernel - _kernel) + (user - _user);   // kernel は idle を含む
if (total > 0) ratio = Math.Clamp((total - dIdle) / (double)total, 0, 1);
```

初回呼び出しは差分が取れないため、値を保存するだけで 0 を返す（`_primed` フラグ）。

### 3.2 メモリ — `GlobalMemoryStatusEx`

`MEMORYSTATUSEX.dwMemoryLoad` がそのまま使用率（%）。
総量・空き容量はツールチップ表示に使う。

### 3.3 GPU — パフォーマンスカウンタ

カテゴリ `GPU Engine`、カウンタ `Utilization Percentage`。
インスタンスはプロセス × エンジン種別で作られ、次のような名前になる。

```
pid_12345_luid_0x00000000_0x0000C8D2_phys_0_eng_0_engtype_3D
pid_12345_luid_0x00000000_0x0000C8D2_phys_0_eng_5_engtype_Compute
```

**エンジン種別で目的が分かれる点が重要。**

| engtype | 何の負荷か |
|---|---|
| `3D` | 描画・ゲーム |
| `Compute` / `Cuda` | **CUDA、DirectML など。深層学習はここ** |
| `Copy` | 転送 |
| `VideoDecode` / `VideoEncode` | 動画のデコード・エンコード |

`engtype_3D` だけを見ていると、**学習中でも 0% に見える**。
本アプリは 3D と Compute を別々の指標として持ち、学習中は Compute を見るよう案内している。

```csharp
protected override bool Match(string instance)
    => instance.Contains("engtype_Compute", StringComparison.OrdinalIgnoreCase)
    || instance.Contains("engtype_Cuda", StringComparison.OrdinalIgnoreCase);
```

該当インスタンスを全て合算し、100 で割って `Ratio` にする（タスク マネージャーと同じ考え方）。

**インスタンスはプロセス単位なので出入りが激しい。**
`Volatile => true` を指定した指標は 5 秒ごとにインスタンスを列挙し直す。
このため、瞬間的な負荷はタスク マネージャーより数秒遅れて出ることがある。

### 3.4 VRAM — 使用量と総量

使用量はカテゴリ `GPU Adapter Memory`、カウンタ `Dedicated Usage`。
アダプタごとにインスタンスが出るため、合算ではなく最大値を採る（`UseMax => true`）。

総量はカウンタから取れないので、ディスプレイ アダプタのレジストリから読む。

```csharp
const string path =
    @"SYSTEM\CurrentControlSet\Control\Class\{4d36e968-e325-11ce-bfc1-08002be10318}";
// 各サブキーの HardwareInformation.qwMemorySize の最大値を採用
```

総量が取れない環境では、観測した最大値を暫定の上限として `Ratio` を出す
（表示は右肩上がりに補正されるが、実値 GB は正しい）。

### 3.5 ディスク・ネットワーク

- ディスク：`PhysicalDisk \ % Disk Time` の `_Total` インスタンス
- ネットワーク：`Network Interface \ Bytes Received/sec`、`Bytes Sent/sec`
  - `Loopback` / `isatap` / `Teredo` は仮想アダプタなので除外
  - Mbps に変換（`bytes * 8 / 1_000_000`）
  - 回線速度は環境依存のため、**観測した最大値を上限として** `Ratio` を出す

### 3.6 温度を扱わない理由

CPU / GPU 温度は WMI の `MSAcpi_ThermalZoneTemperature` や
ベンダー固有ドライバ経由でしか取れず、いずれも管理者権限か追加ライブラリ
（LibreHardwareMonitorLib 等）を必要とする。
「管理者権限を要求しない」方針と両立しないため、対象外とした。

---

## 4. カウンタ名のローカライズ問題

**パフォーマンスカウンタのカテゴリ名・カウンタ名は OS の表示言語でローカライズされる。**
日本語 Windows では次のコードは例外になる。

```csharp
new PerformanceCounter("GPU Engine", "Utilization Percentage", instance);   // 日本語環境では通らない
```

Windows はレジストリに、英語名と現在言語名の対応表を index 付きで持っている。

```
HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Perflib\009             ← 英語 (LCID 9)
HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Perflib\CurrentLanguage ← 現在の表示言語
```

どちらも `REG_MULTI_SZ` で `[index, 名前, index, 名前, ...]` の並び。
同じ index 同士を突き合わせれば「英語名 → 現在言語名」の辞書が作れる。

```csharp
// どちらも [index, name, index, name, ...] の並び
var byIndex = new Dictionary<string, string>();
for (int i = 0; i + 1 < localList.Length; i += 2)
    byIndex[localList[i]] = localList[i + 1];

for (int i = 0; i + 1 < englishList.Length; i += 2)
    if (byIndex.TryGetValue(englishList[i], out string? localized))
        result[englishList[i + 1]] = localized;
```

コード側は常に英語名で書き、`PerfNames.Localize()` を通してから使う。
辞書が作れなかった場合は英語名のまま使う（英語環境では正しく動く）。

---

## 5. アイコンの描画

### 5.1 表示サイズは DPI で変わる

通知領域のアイコンの実表示サイズは画面の拡大率で変わる。

| 拡大率 | 実サイズ |
|---|---|
| 100% | 16 × 16 px |
| 150% | 24 × 24 px |
| 200% | 32 × 32 px |

**大きく描いて Windows に縮小させると必ずぼやける。**
`SystemInformation.SmallIconSize` を取得し、実サイズちょうどに描く必要がある。

### 5.2 3 モード共通の描画手順

数字・キャラ・画像のどれも、**16×16 の論理グリッド `char[][]` を組み立ててから、
実表示サイズへ敷き詰める**という同じ手順に乗せている。

```csharp
private static Icon Render(char[][] grid, Color tone)
{
    int size = Math.Clamp(SystemInformation.SmallIconSize.Width, 16, 64);

    using var bmp = new Bitmap(size, size);
    using (var g = Graphics.FromImage(bmp))
    {
        g.Clear(Color.Transparent);
        g.SmoothingMode = SmoothingMode.None;              // ドットを保つ
        g.InterpolationMode = InterpolationMode.NearestNeighbor;
        g.PixelOffsetMode = PixelOffsetMode.Half;
        PaintSprite(g, grid, size, tone);
    }
    return ToIcon(bmp);
}
```

セルの割り当ては floor で行い、割り切れないサイズ（24px = 1.5 倍）でも
隙間や重なりが出ないようにしている。

```csharp
double cell = size / (double)G;
int x0 = (int)Math.Floor(x * cell);
int w  = Math.Max(1, (int)Math.Floor((x + 1) * cell) - x0);
```

グリッドの文字は色の役割を表す。

| 文字 | 役割 |
|---|---|
| `C` | 指標の色 |
| `L` / `c` | 明るい版 / 暗い版（上下の陰影で球体に見せる） |
| `H` | 高負荷時のゲージ（赤） |
| `K` `W` | 目・口 / 白 |
| `P` `B` | ほっぺ / 汗 |
| `D` | ゲージの下地 |
| `.` | 透明 |

### 5.3 数字モードは自前のドット字形

GDI+ の文字描画は 16px では潰れるため、**5×9 のドット字形を内蔵**している。

```csharp
['4'] = new[] { "...#.", "..##.", ".#.#.", "#..#.", "#####", "...#.", "...#.", "...#.", "...#." },
```

描いたあと、白ドットに接する透明セルを 1 ドットぶん暗く塗って縁取りする。
これでタスクバーが明色でも暗色でも読める。

```csharp
private static void Outline(char[][] grid)
{
    // 走査中に書き換えると縁が縁を呼んで太るので、集めてから塗る
    var edges = new List<(int Y, int X)>();
    ...
    foreach ((int y, int x) in edges) grid[y][x] = '#';
}
```

字間は 1 ドットだが、3 文字（`9.9` や `99+`）で 16px に収まらない場合のみ 0 に詰める。
縁取りぶきまで数えて詰めると、`99` の 9 と 9 が繋がって 1 文字に見えてしまうため、
**端の縁取りが欠けるほうを許容している**。

### 5.4 高負荷の表現

当初は高負荷時にアイコン本体を赤に寄せていたが、**これは失敗だった**。
補色を混ぜることになるため、

- 青（CPU）× サーモン → 灰色
- ミント（VRAM）× サーモン → 茶色

となり、100% ではどの指標も似た色に潰れる。
**一番「どれが限界なのか」を知りたい瞬間に、見分けがつかなくなる。**

現在は本体の色を指標の色として保ち、危険は
**下部ゲージの赤（`'H'`）と、キャラの表情**で伝えている。

### 5.5 指標どうしの関係を表情にする

アイコンは 1 つずつ独立して描かれるが、**表情だけは他の指標の状況を見て決める**。

`IconMood` に「自分以外でいちばん高い使用率が 80% を超えているか」を入れて渡し、
自分が 20% 未満（= 暇）かつ他が限界なら、眠らずに「待ちぼうけ」の顔にする。

```csharp
// 自分が 1 位なら 2 位が、そうでなければ 1 位が「隣の忙しさ」になる
double othersPeak = sample.Ratio >= top1 ? top2 : top1;
var mood = new IconMood(_tick, _recorder.Recording, othersPeak >= 0.80);
```

これは見た目のためだけの機能ではない。**GPU が待ちぼうけ + CPU が限界**は
データ供給が追いついていないという意味で、学習が遅い原因の切り分けに直接使える。
数字を 2 つ見比べて頭で判断する代わりに、表情で目に入るようにしている。

実装上の注意として、この顔だけは左右対称の `Pair()` を使えない。
両目を同じ向きへずらす必要があるためで、対称にずらすと目が離れて驚き顔になる。

```csharp
// ここだけは Pair を使わない。対称にずらすと目が離れて驚き顔になる
OvalEye(2, 5);
OvalEye(9, 5);
```

### 5.6 GDI ハンドルの解放

`Bitmap.GetHicon()` が返すハンドルは GDI リソースであり、
**`DestroyIcon` で明示的に解放しないと漏れる**。毎秒生成しているため、
放置すると数時間でハンドルを食い潰してアイコンが描画されなくなる。

```csharp
private static Icon ToIcon(Bitmap bmp)
{
    IntPtr handle = bmp.GetHicon();
    try
    {
        using var temp = Icon.FromHandle(handle);
        return (Icon)temp.Clone();   // ハンドルから独立したコピーを返す
    }
    finally
    {
        DestroyIcon(handle);
    }
}
```

差し替えた古い `Icon` オブジェクトも毎回 `Dispose` している。

---

## 6. 通知領域のアイコンの扱い

### 6.1 Windows 11 は新しいアイコンを隠す

Windows 11 は、初めて見るトレイアイコンを既定でオーバーフロー（∧ の中）に入れる。
**配布相手が exe をダブルクリックしても、タスクバーには何も現れない。**
「動いていない」と受け取られるため、配布でもっとも障害になる箇所である。

Windows 11 はアイコンごとの表示設定を次の場所に持っている。

```
HKCU\Control Panel\NotifyIconSettings\<ハッシュ>
    ExecutablePath : exe のフルパス
    UID            : プロセス内での通し番号
    IsPromoted     : 1 = タスクバーに出す, 0 = オーバーフロー
```

`IsPromoted` に 1 を書けば表に出せる。HKCU なので管理者権限は不要。
Windows 10 にはこのキーが無いため、その場合は手動手順を画面に出す。

**このエントリはアイコンを一度表示しないと作られない**ため、
起動直後ではなく、最初のタイマー tick（1 秒後）以降に処理する。

### 6.2 アイコンの同一性を保つ

Windows はトレイアイコンを **exe のパス + UID** で識別する。
WinForms の `NotifyIcon` はこの UID を、プロセス内で `NotifyIcon` を生成した順に
1 から振っていく（静的カウンタ）。

つまり、**表示の ON/OFF で `NotifyIcon` を作り直すと UID がずれる**。
すると `IsPromoted` を書いた設定は古いエントリに取り残され、
アイコンは再びオーバーフローに引っ込んでしまう。

開発中に実際にこれが起き、レジストリを見ると
「本来 3 番目の指標である GPU(3D) が UID 6 に居る」という形で現れた。

対策として、**起動時に全指標ぶんの `NotifyIcon` を同じ順序で作り、
以後は `Visible` だけを切り替える**。これで UID は指標ごとに毎回同じになる。

```csharp
private void CreateSlots()
{
    foreach (Metric metric in _metrics)      // 常に MetricCatalog の順で全部作る
    {
        var notify = new NotifyIcon { ContextMenuStrip = _menu, Text = metric.Name };
        _slots[metric.Id] = new Slot { Metric = metric, Notify = notify };
    }
    SyncSlots();                             // 表示 ON/OFF は Visible だけで行う
}
```

### 6.3 見失わないための多重の保険

アイコンを全部消すと右クリックする先が無くなり、設定に戻れなくなる。
復帰手段を複数用意している。

1. **Ctrl+Alt+M**（`RegisterHotKey` によるグローバルホットキー）で表示をトグル
2. **exe をもう一度起動**すると、名前付き Event 経由で常駐側が設定画面を開く
3. **「表示する項目」は最後の 1 つを外せない**ようガードしている
4. **スタートメニューのショートカット**を初回起動時に作成（終了後も探し出せる）
5. **「終了」時に確認**し、戻しかたと exe の場所を表示する

ショートカット作成は COM の `WScript.Shell` に ProgID からの遅延バインドで依頼している。
参照ライブラリを増やさないための選択で、単一ファイル発行でも動作することを確認済み。

---

## 7. 計測とデータ出力

`SessionRecorder` が毎秒の `Sample` を蓄積する。
長時間の実行でメモリを食い潰さないよう、**上限に達したら 1 つおきに間引き、
以降の記録間隔も倍にする**（解像度を半分に落として全区間を保つ方式）。

```csharp
private const int MaxSamples = 20000;   // 1 秒間隔なら約 5.5 時間ぶん

private void Thin()
{
    for (int i = _times.Count - 1; i >= 0; i -= 2) _times.RemoveAt(i);
    ...
    _skip *= 2;
}
```

結果画面では折れ線グラフと、平均・最大・最小を表示する。
グラフは描画幅を超えるサンプルを間引いて描く（`stride = count / plotWidth`）。

CSV は `時刻, 経過秒, 各指標の実値` の形式。
Excel で文字化けしないよう **BOM 付き UTF-8** で書き出している。

```csharp
File.WriteAllText(dialog.FileName, text.ToString(), new UTF8Encoding(true));
```

---

## 8. 設定の保存先

| 場所 | 内容 |
|---|---|
| `HKCU\Software\TaskbarMeter` | 表示モード、しきい値、見た目、指標ごとの ON/OFF と色 |
| `HKCU\...\CurrentVersion\Run` | 自動起動（ユーザーが有効にした場合のみ） |
| `HKCU\Control Panel\NotifyIconSettings` | タスクバーへの表示（`IsPromoted`） |
| `%APPDATA%\...\Start Menu\Programs\TaskbarMeter.lnk` | スタートメニューのショートカット |
| `%LOCALAPPDATA%\TaskbarMeter\skin.png` | 画像から作ったアイコン素材 |

いずれもユーザー領域のみ。**HKLM や Program Files には一切書き込まない。**
アンインストールは、上記を削除して exe を消すだけで完了する。

読み書きはすべて例外を握りつぶす方針にしている。
設定が保存できなくても、そのセッションでは動き続けるほうが望ましいため。

---

## 9. 既知の制約

| 制約 | 内容 |
|---|---|
| GPU の反応が数秒遅れる | インスタンスがプロセス単位で出入りするため、5 秒ごとに列挙し直している |
| 100% は `99` と表示 | 16px に 3 桁が入らない。ゲージは満タンになる |
| 温度・ファン回転数は非対応 | 管理者権限か追加ライブラリが必要（§3.6） |
| ネットの `Ratio` は相対値 | 回線速度が取れないため、観測した最大値を上限としている |
| SmartScreen 警告が出る | コード署名証明書が無いため。「詳細情報」→「実行」で回避 |
| UID の採番は WinForms の実装依存 | §6.2 の前提が将来変わった場合は、表示設定をやり直す必要がある |

---

## 10. ビルド

.NET 8 SDK が必要。

```
build.cmd
```

「常駐プロセスの終了 → `dotnet publish -c Release` → 新しい exe を起動」までを行う。
**常駐中の exe はロックされるため、必ず先にプロセスを落とす必要がある。**

出力：`bin\Release\net8.0-windows\win-x64\publish\TaskbarMeter.exe`

### 見た目を変更したときの検証方法

アイコンはコードを読んでも良し悪しが判断できないため、実際に描いて確認する。
`Program.cs` をリンクし、`StartupObject` を差し替えた使い捨てプロジェクトを作れば、
本体を汚さずに `IconFactory` を呼べる。

```xml
<Compile Include="..\TaskbarMeter\Program.cs" Link="Program.cs" />
<StartupObject>IconSheet.Entry</StartupObject>
```

`IconFactory.Create(...)` の結果を `Icon.ToBitmap()` して最近傍で 8 倍に拡大し、
負荷（5 / 35 / 65 / 90 / 100%）× モード × 色 の一覧を PNG に出力すると判断しやすい。
この方法で「100% で全色が同じ赤に潰れる」「`99+` が読めない字になる」を発見した。

なお、フォームの確認に `Control.DrawToBitmap()` を使うと、入れ子パネルの位置を
取り違えて描画される。実際に `Show()` してから `Graphics.CopyFromScreen` で
画面のピクセルを撮ること。

---

## ほかのページ

| | |
|---|---|
| **[はじめかた.md](はじめかた.md)** | 導入して使いはじめるまで |
| **[説明書.md](説明書.md)** | 全機能の説明と、困ったときの対処 |
| [README](README.md) | このアプリの概要とダウンロード |
